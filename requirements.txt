# MAROS Meals — Python dependencies.
#
# This project is separate from the MAROS backend but imports code from it at
# runtime (chipper's cached Whisper model, podcastengine's TTS and LLM
# router). Point MAROS_ROOT at that checkout in .env. The relationship is
# one-way and read-only.

# ── Config ───────────────────────────────────────────────────────────────
python-dotenv==1.2.3
requests==2.34.2

# ── Meal validation ──────────────────────────────────────────────────────
jsonschema==4.26.0

# ── Narration ────────────────────────────────────────────────────────────
edge-tts==7.2.8
gTTS==2.5.4
pydub==0.25.1
# pydub imports audioop, which was removed from the stdlib in Python 3.13.
audioop-lts==0.2.2 ; python_version >= "3.13"

# ── Forced alignment ─────────────────────────────────────────────────────
faster-whisper==1.2.1
onnxruntime==1.29.0

# ── Media ────────────────────────────────────────────────────────────────
moviepy==2.2.1
numpy==2.5.2
pillow==11.1.0

# ── clipper (the older two-character explainer pipeline) ─────────────────
pymupdf==1.28.2
pydantic==2.13.4

# — clipper YouTube frontend/server —
fastapi==0.121.2
uvicorn[standard]==0.38.0
yt-dlp==2026.8.19
